---
description: How to start the backend dev server
---
# Start Dev Server

To run the backend server under normal conditions:
1. Ensure you are in the `c:\projects\SimpleTestApp\backend` directory.
2. Run `node server.js` to start the server.
3. The server runs on port 3080 by default (`app.listen(3080)`).
